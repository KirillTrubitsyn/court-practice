# MCP SDK (включает FastMCP). Streamable HTTP transport поддерживается с 1.7+.
# Берём последнюю стабильную: 1.27 умеет streamable_http_app() и custom_route.
mcp[cli]==1.27.0

# ASGI + сервер
starlette==0.41.3
uvicorn[standard]==0.32.1

# Конфиг. mcp>=1.27 требует pydantic>=2.11, поэтому держим свежие.
pydantic==2.13.3
pydantic-settings==2.14.0

# HTTP клиент для Voyage
httpx==0.28.1

# Поиск
rank-bm25==0.2.2
pymorphy3==2.0.3
pymorphy3-dicts-ru==2.4.417150.4580142
numpy==2.1.3

# Кэш
redis==5.2.1

# Логи
structlog==24.4.0

# Утилиты
tqdm==4.67.1
