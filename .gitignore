__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.local
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
dist/
build/

# Артефакты reference-скилла, которые НЕ должны попасть в git
data/.voyage_config.json
data/query_cache.pkl
data/court_practice_db.json

# Артефакты пересборки. Текущие index.pkl.gz и embeddings.npy уже коммичены —
# чтобы их не перезаписать случайно при ребилде в продакшене, опционально
# раскомментируй:
# data/index.pkl.gz
# data/embeddings.npy
